import DiceaseData from "@/components/DiceaseData";
import Hero from "@/components/Hero";

const KidneyHome = () => {
  return (
    <div>
      <Hero />
      <DiceaseData />
    </div>
  );
};

export default KidneyHome;
